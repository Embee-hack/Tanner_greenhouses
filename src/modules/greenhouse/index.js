import { pagesConfig } from "@/pages.config";

export const greenhousePages = pagesConfig.Pages;
export const greenhouseLayout = pagesConfig.Layout;
export const greenhouseMainPage = pagesConfig.mainPage;
